color(c)
  path
    fill    c
    stroke  c

.map
  .hull
    path
      opacity     0.15  // transparent so the intersection is more opaque
  .gov
    color green
  .eu
  .un
    color orange
  .trilat
    color blue
