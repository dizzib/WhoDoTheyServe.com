overlay(id, color)
  .badge-{id}
    text
      fill        color
  .edge.{id}
    stroke        color
    stroke-width  1px

.graph
  overlay('ac' , blue)
  overlay('bis', red)
  overlay('cfr', magenta)
